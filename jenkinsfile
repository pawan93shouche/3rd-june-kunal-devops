pipeline {
    agent { node { label 'built-in' } }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
		stage ('bye') {
		    steps {
			    echo ' Good Bye'
			}
		
		}
		
    }
}
